# オンライン対戦サーバー（Colyseus）の WebSocket URL
# 本番（Vercel）では必ず設定すること。未設定時は設定エラーを表示します。
# 開発時の未設定は、同一オリジンの /colyseus を通じてローカルサーバー（2567）へ接続します。
# 例: VITE_COLYSEUS_URL=wss://your-app-1234.colyseus.cloud
VITE_COLYSEUS_URL=
